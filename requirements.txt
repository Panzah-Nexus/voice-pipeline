# Core Pipecat with WebSocket server + Silero VAD
numpy>=1.26.4
pipecat-ai[websocket,silero]

# FastAPI / Uvicorn
uvicorn[standard]

# Audio helpers
soundfile

# Optional GPU-side 8-bit quantisation (saves VRAM if you need it)
bitsandbytes
# Test comment
