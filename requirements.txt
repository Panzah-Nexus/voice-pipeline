# Core Pipecat framework
pipecat-ai==0.0.71

# Local Speech-to-Text (Whisper)  
pipecat-ai[whisper]

# Local Language Model (Ollama)
pipecat-ai[ollama]

# Audio processing and VAD
pipecat-ai[silero]

# WebSocket transport  
pipecat-ai[websocket]

# Kokoro (PyTorch build, ≈ 0.5.2 or newer auto-discovers weights in $KOKORO_HOME)
kokoro>=0.9.4

# FastAPI / Uvicorn
uvicorn[standard]

# Audio helpers
soundfile>=0.12.1

# Optional GPU-side 8-bit quantisation (saves VRAM if you need it)
bitsandbytes
# Test comment
