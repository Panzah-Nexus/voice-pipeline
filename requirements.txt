# Stable, conflict-free dependencies for the voice pipeline.

# 1. AI Services & Frameworks
pipecat-ai[whisper,websocket,silero]~=0.0.71
kokoro-onnx~=0.4.9
# No longer using moonshine, but keeping this as a reference
# useful-moonshine-onnx@git+https://github.com/usefulsensors/moonshine.git#subdirectory=moonshine-onnx

# 2. ONNX Runtime for GPU
# This is the core component. We install the GPU version explicitly.
# The version is chosen to be compatible with CUDA 12.x and other packages.
onnxruntime-gpu==1.22.0

# 3. Core Dependencies for Whisper (via faster-whisper)
# We specify ctranslate2 with CUDA support to ensure GPU acceleration.
ctranslate2[cuda]~=4.6.0
faster-whisper~=1.1.1
av~=14.4.0
tokenizers<1,>=0.13

# 4. Core Pipecat & System Dependencies
# numpy is now handled by kokoro-onnx's dependency
protobuf~=5.29.3
aiohttp~=3.11.12
loguru~=0.7.3
pydantic~=2.10.6
python-dotenv~=1.0.1
uvicorn~=0.30.5
fastapi~=0.112.0
websockets~=12.0
phonemizer~=3.2.1

# 5. Other transitive dependencies
# These are often pulled in by the packages above, but we list them
# here for clarity and to ensure a stable build.
pyloudnorm~=0.1.1
resampy~=0.4.3
soxr~=0.5.0.post1
openai~=1.70.0
huggingface-hub>=0.13
pyyaml<7,>=5.3
tqdm
typing-extensions
# Dependencies for onnxruntime
coloredlogs
flatbuffers
packaging
sympy
mpmath
