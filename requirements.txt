# Core Pipecat with Ultravox + WebSocket server + Silero VAD
pipecat-ai[ultravox,websocket,silero]

# Kokoro (PyTorch build, ≈ 0.5.2 or newer auto-discovers weights in $KOKORO_HOME)
kokoro>=0.9.4

# FastAPI / Uvicorn
uvicorn[standard]

# Audio helpers
soundfile>=0.12.1

# Optional GPU-side 8-bit quantisation (saves VRAM if you need it)
bitsandbytes
